
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa

  h2:
    console:
      enable: true
      path: /h2-console

  jpa:
    properties:
      hibernate:
        format_sql: true
        user_sql_cooments: true

    hibernate:
      ddl-auto: create


  profiles:
    include: jwt



logging:
  level:
    org:
      apache:
        coyote:
          http11: debug

      hiberante:
        SQL: debug

    boardexample:
      myboard: info

